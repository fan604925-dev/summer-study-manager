# 2026-07-23 工作日志

## 修复：全新打开链接积分清零（严重数据丢失风险）
- **根因**：`localStorage` 在隐私模式 / iOS 主屏幕App(standalone) / 部分 WebView 关闭后会丢失；旧 `save()` 的 `try/catch` 静默吞掉写入失败，数据只存内存 → 关闭即丢。用户"点恢复网页才保留"= 旧内存进程存活现象。
- **修复方案**：`js/store.js` 增加 IndexedDB 冗余持久化
  - `save()` 同步写 localStorage + 异步写 IndexedDB（库 `ssm-db-v1`）
  - `init()` 末尾 `_syncIdb()`：先备份内存到 IDB；localStorage 缺失时从 IDB 恢复并一次性 reload（sessionStorage 防循环）
  - `clearData()` 末尾调 save，同步清 IDB 备份
  - `_lsAvailable` 标记 localStorage 可用性，禁用时自动切换 IDB
- **验证**：`node --check` 语法通过；WebFetch 确认 GitHub Pages 已上线含 `_syncIdb`
- **部署**：commit `6be04de`，已 push 到 `fan604925-dev/summer-study-manager` main；GitHub Pages 自动重建

## 待用户操作（数据迁移到新备份层）
- 用户当前数据在"恢复网页"内存中（localStorage 可能空）。需让数据进入 IndexedDB：
  - 在能看见数据的会话里，去"任务"页勾选任意一个任务（或"奖励"页兑换一次）→ 触发 save → 写入 IDB
  - 之后全新打开链接即可从 IDB 自动恢复
- 建议用户用**普通浏览器标签页**打开（勿隐私模式、勿主屏幕App模式）；设置页「导出数据」作终极备份

## 新增功能：任务删减（删除）
- **需求**：孩子要求任务里要有"删减"功能，之前只有"增加"(FAB +临时任务)
- **实现**（`tasks.html` + `js/store.js` + `css/style.css`）：
  - 列表项右侧新增 🗑️ 快速删除按钮；任务详情弹窗底部新增「删除任务」按钮
  - 自定义任务(`custom:true`)：直接移除模板+所有实例，不会复活
  - 默认任务：删除后记入 `deletedTemplateIds`，`_repairData` 在"整体重填"与"合并补充"两条分支后都过滤掉已删 id → **版本升级/修复都不会让删掉的任务复活**
  - 已获得的积分**不扣回**（保护孩子已得成果）
  - 删除前有 `app.confirm` 二次确认
- **持久化**：新增 `STORAGE_KEYS.DELETED_TEMPLATES='ssm_deletedTemplates'`，存入 localStorage + IndexedDB（随 _serializeAll 同步）
- **未改 DATA_VERSION**：未动默认模板/奖励结构，旧用户无需触发 repair；deletedTemplateIds 缺失时默认 `[]`
- **测试**：`test_store.js` 新增 15.x 段（自定义删除、默认删除后 repair 不复活），全部 51 项通过
- **部署**：commit `94fb64b`，已 push main；WebFetch 缓存破坏参数确认 `deleteTaskTemplate` 已上线
- **注意**：删除是永久的（默认任务可用设置页「恢复默认数据/清除数据」整体重置找回；或手动重新添加）
