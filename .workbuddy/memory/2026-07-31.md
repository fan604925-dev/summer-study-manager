# 2026-07-31

## 改动 v23：笔记整理归为学习项（修复误判为家务）
- 根因：`KNOWN_CHORE_NAMES` 含「整理」，而「猿辅导语文/数学/英语笔记整理」名字含「整理」→ 旧 `_repairData` 自动识别逻辑把它们标记为 `cashable:true`（家务积分）。实例同步又用 `inst.cashable = tmpl.cashable || inst.cashable`，导致旧实例一旦被误标就永久错池。
- 修复（`js/store.js`）：
  1. 自动识别家务改为「仅『其他』科目 + 名字命中家务词 + 非笔记」；名字含「笔记」一律学习项（含显式剥离已有 cashable）。
  2. 实例同步改为以模板为准：`inst.cashable = !!tmpl.cashable && !tmpl.name.includes('笔记')`，修复旧实例错池。
  3. `DATA_VERSION` 21→22，强制所有老用户重跑 `_repairData` 修正已错标的笔记实例/模板。
- `service-worker.js`：`CACHE_NAME` 升 `ssm-v11`。
- 测试：`test_store.js` 新增 18.3（笔记非家务、完成只加学习积分），全部通过。
- commit `b563345`，已 push main，GitHub Pages 1-5 分钟生效。

## 家务识别约束（写进 MEMORY）
- 笔记整理 = 学习项，永远不算家务；`KNOWN_CHORE_NAMES='整理'` 仅作用于「其他」科目，且显式排除「笔记」。

## 改动 v24：清爽配色 UI 重设计
- 用户诉求：更清爽、干净，提升视觉效果。
- `css/style.css` 引入 `:root` 设计令牌（--brand #2f80ed、--grad-brand 蓝→青渐变、--bg 浅蓝灰、--surface 白、--border 细线、--shadow 轻阴影、--radius 圆角）；重做 `.card`/`.child-card`/`.task-item`/`.btn-*`/`.fab`/`.countdown-box`/`.points-display`/`.bottom-nav`/`.sticky-header`（毛玻璃+细边框），背景加顶部柔光径向渐变。
- 全站散落的 `#3b82f6` 替换为新品牌色/渐变（index/tasks/rewards/settings/sprint/stats 的 theme-color、accent-color、Chart 配色、进度条、边框、标签）。
- `service-worker.js`：`CACHE_NAME` 升 `ssm-v12`，强制拉取新样式。
- 测试：`test_html_syntax.js` 全过（逻辑未动，`test_store.js` 维持通过）。
- commit `bec1073`，已 push main，GitHub Pages 1-5 分钟生效。
- 配色基调：白底 + 蓝青渐变点缀 + 柔和阴影，避免重色块，整体更透气。

